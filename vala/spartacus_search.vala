int[] spartacus_search(int[] haystack, int needle) {
    foreach (int x in haystack) stdout.printf("%d\n", x);
    return haystack;
}

int main() {
    int[] h = {1, 2, 3, 4, 5};
    spartacus_search(h, 3);
    return 0;
}
